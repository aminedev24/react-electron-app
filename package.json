{

  "name": "react-electron-app",
  "version": "1.0.0",
  "description": "this is react electron crud app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --trace-warnings ",
    "build": "electron-builder build --config electron-build.json"
    
  },
  "author": "abdennour",
  "license": "ISC",
  "devDependencies": {
    "electron": "^26.3.0",
    "electron-builder": "^24.6.4"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
